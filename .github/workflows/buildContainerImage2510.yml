name: Docker Container for Ubuntu 25.10
on:
  workflow_dispatch:
  push:
permissions:
  contents: read

jobs:
  build:
    uses: n138-kz/Dockerfile.ubuntu/.github/workflows/docker-image_ubuntu2510.yml@main
    secrets: inherit
